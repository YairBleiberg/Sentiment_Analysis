This project implements Sentiment Analysis on the Sentiment Treebank dataset by Stanford, using Neural Networks (in PyTorch). We use 2 models - LogLinear and LSTM, with 1-hot and Word2Vec embeddings.
